meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  persistenceMount: /data
  image: null
  command: null
  args: null
  containerPort: 8000
serviceType: compute
