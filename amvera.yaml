meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  persistenceMount: /data
  image: null
  command: null
  args: null
  containerPort: 80
serviceType: compute
